{
  "name": "codespace-dashboard",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "TypeScript source for the CodeSpace embedded dashboard. Compiled output is checked into dist/ and embedded into the cse binary via include_str!.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
